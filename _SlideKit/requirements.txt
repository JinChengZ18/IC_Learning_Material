# _SlideKit 运行依赖。安装：python -m pip install -r requirements.txt
matplotlib>=3.7      # 绘制架构/原理图、逐页版面预览，并导出 PNG+SVG（含其依赖 numpy）
python-pptx>=1.0     # 生成可编辑 .pptx（原生标题/要点文本 + 母版/版式 XML）
Pillow>=10           # 读图尺寸以等比放入幻灯片（deck._pic）、生成缩略图（tools/thumbs.py）
